/**
 * Environment configuration for the development mode.
 * This configuration includes the API base URL and other environment-specific settings.
 */
export const environment = {
    production: false, // Indicates if the app is running in production mode
    apiUrl: 'https://dev.brain-backend.dainstudios.com', // Base API URL for development
};
