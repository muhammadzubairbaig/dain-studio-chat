/**
 * Enum to define user roles in the chat application.
 * This helps to maintain consistent role references across the app.
 */
export enum Role {
    User = 'user',
    Assistant = 'assistant',
}
